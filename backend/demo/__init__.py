"""Demo mode package for AI Autonomous Lecturer.

When ``DEMO_MODE=true`` is set in ``.env``, this package provides mock
implementations of hardware-dependent components (camera, microphone, students)
so the system can run without any real hardware — ideal for expo demonstrations.
"""
