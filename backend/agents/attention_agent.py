"""Attention monitoring agent placeholder.

TODO PR2: Implement:
  - BERT/DistilBERT-based sentiment and attention classification.
  - Rolling 30-second attention score window per student.
  - Trigger distraction events when attention_score < 0.3 for > 60s.
  - Feed attention updates into LectureState.
"""
