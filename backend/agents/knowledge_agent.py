"""Knowledge agent placeholder (RAG knowledge retrieval).

TODO PR2: Implement:
  - LlamaIndex + ChromaDB ingestion of data/knowledge_base/ documents.
  - Semantic search via sentence-transformers (all-MiniLM-L6-v2).
  - query(query_text, top_k) method used by knowledge_tools.py.
  - add_document(file_path) for runtime document ingestion.
  - get_topic_outline(topic) to build lecture outline from knowledge base.
"""
